// Function to find the percentage of a number, given the number and a percentage.
function percentageCalculator(number, percentage) {
    return number * percentage / 100;
}

let percent1 = percentageCalculator(60, 10);
let percent2 = percentageCalculator(350, 17.5);
console.log(percent1);
console.log(percent2);